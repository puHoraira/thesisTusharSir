# auto uses the current platform. Set mobile or web to force a mode for testing.
APP_MODE=auto
# Backend server endpoint
# Use your computer's local network IP (same as Metro bundler)
SERVER_ENDPOINT=http://192.168.0.102:8000
# Google Maps API Key (optional - for map features)
GOOGLE_MAPS_API_KEY=
